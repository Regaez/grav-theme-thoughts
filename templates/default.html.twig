{% extends 'partials/base.html.twig' %}

{% block content %}
  <div class="header">
    <h1>{{ page.title }}</h1>
    {{ page.header.preface|markdown }}
  </div>

  <div class="content">
    {{ page.content }}
  </div>
{% endblock %}
